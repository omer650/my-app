server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # זוהי שורת הקסם: אם הקובץ לא קיים, תחזיר את index.html
        try_files $uri $uri/ /index.html;
    }
}